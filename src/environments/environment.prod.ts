export const environment = {
  production:   true,
  strapiUrl:    '',           // vide = proxy Express SSR (/api → Strapi)
  larcoApiBase: '/larco-api', // proxy Express SSR (/larco-api → api-geadaie...)
};
